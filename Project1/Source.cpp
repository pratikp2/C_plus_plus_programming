#include <iostream>
#include <string>
#include <map>
#include <vector>

using namespace std;

vector<string> Findconcatination(vector<string> source, int N)
{
	vector<string> vec;

	for (int i = 0; i < source.size(); i++)
	{

	}
}

int main()
{
	vector<string> source{ "cat", "dog", "bird", "snow", "snowbird", "tail", "hot", "tailor", " or ", "hotdog" };
	Findconcatination(source, 6);
	return 0;
}